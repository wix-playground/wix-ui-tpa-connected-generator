#!/usr/bin/env node

// TODO: Not finished

/*
const path = require('path')
const args = process.argv.slice(2)

if (!args[0]) {
  console.error('Please provide output path!')
  process.exit(1)
}

new (require('./dist/index')).Analyser(path.resolve(process.cwd())).exportComponentConfig(path.resolve(args[0]))
*/
