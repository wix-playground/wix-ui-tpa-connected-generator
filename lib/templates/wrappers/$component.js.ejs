import React from 'react'
import {<%=componentName%>Ext} from '../../<%=consumerBasePath%>/<%=componentName%>/<%=componentName%>'

let projectVariableStructure = <%-projectVariableStructure%>
let styles = '<%=COMPONENT_STYLES_PLACEHOLDER%>'

// Avoiding tree-shaking - remove this block once variables are used
projectVariableStructure = projectVariableStructure
styles = styles

export class <%=componentName%> extends React.Component {
  render() {
    return React.createElement(<%=componentName%>Ext, {...this.props})
  }
}
